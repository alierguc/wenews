import React,{Component} from 'react'
import {URL,API_KEY} from '../constants/configconst';

export default function RestService() {
    getNewsApi = (source,author,title,description,url,urlToImage,publishedAt,content) =>{
        
    }
}
